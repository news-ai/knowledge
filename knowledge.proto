syntax = "proto3";

package knowledge;

service KnowledgeManager {
    rpc EntityExtraction (EntityRequest) returns (EntityReply) {}
}

message EntityRequest {
    string topic = 1;
}

message EntityReply {
    string response = 1;
}